{
  "name": "alma-telegram-bridge",
  "version": "1.0.0",
  "description": "Telegram <-> Alma bidirectional bridge plugin",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  },
  "dependencies": {
    "alma-plugin-api": "^0.1.0"
  }
}
